/**
 * dk-chat-nav — host half (empty).
 * This is a browser-surface decoration plugin; all logic lives in the client
 * bundle. The host half exists so the plugin row activates in the loader,
 * which the client-modules scanner keys on.
 */
export function apply() {}
